
export const Login = ({ handleClick }: any) => {
    
    return (
        <>
            <button
            className="login-button"
            onClick={handleClick} 
            >Login with signer</button>
        </>
    )
}