
export const Login = ({ onClick }: any) => {
    
    return (
        <>
            <button
            className="login-button"
            onClick={onClick} 
            >Login with signer</button>
        </>
    )
}