// feed. to fix:
ensure profileByID component links to npub of profile image clicked.
render mentions correctly
render note embeds
chronolog_timewarp

note duplicates being rendered. most likey to do with "e" tag check.

when a note is a reply, show who the note is replying to, and link to the note.

video size not rendeirng correctly. 

// App.tsx
// problem: if i do not click login button and refresh the app, the pubkey is not stored in state.

login:
auto-load a feed on start, so no login initially required.
create new keys

move regex text function to helperFunctions component, so can be utilised all through the app by import.